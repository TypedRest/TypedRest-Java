mode: ContinuousDeployment

branches:
  # Mainline branches
  develop:
    label: alpha
    increment: Patch
  master: 
    label: beta

  # Stabilization branches
  release:
    label: rc
  hotfix:
    label: rc
